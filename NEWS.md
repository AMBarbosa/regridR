# regridR 0.2

-   regrid()

    - fixed field names error when `exactextract = TRUE`
    

-   README, regrid.Rd, linkbuild.Rd, downloadif.Rd

    - text improvements


-   NEWS.md

    - new file
    

# regridR 0.1

-   Packaged the following functions, together with documentation and users guide:

    -   `regrid()`
    -   `linkbuild()`
    -   `downloadif()`
